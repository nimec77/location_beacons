name: location_beacons_platform_interface
description: "A common platform interface for the location_beacons plugin."
version: 0.0.1+1
publish_to: none

environment:
  sdk: '>=3.2.3 <4.0.0'
  flutter: '>=3.3.0'

dependencies:
  geolocator: ^11.1.0
  plugin_platform_interface: ^2.1.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  mockito: ^5.4.4
